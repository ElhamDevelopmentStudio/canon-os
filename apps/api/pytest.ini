[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = tests.py test_*.py *_tests.py
addopts = --strict-markers --strict-config
markers =
    e2e: tests that exercise external local services such as PostgreSQL and Redis
